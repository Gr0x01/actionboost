import OGImage, { alt, size, contentType } from './opengraph-image'

// Must declare runtime directly - can't re-export
export const runtime = 'edge'
export { alt, size, contentType }
export default OGImage
